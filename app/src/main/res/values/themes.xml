<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SevenWindsCoffe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SevenWindsCoffe" parent="Base.Theme.SevenWindsCoffe" />

    <style name="ToolBarStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/dark_brown_color</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="RoundedEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:backgroundTint">@null</item>
        <item name="android:background">@drawable/background_edittext_shape</item>
        <item name="android:textColor">@color/light_brown_color</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColorHint">@color/light_brown_color</item>
    </style>
</resources>